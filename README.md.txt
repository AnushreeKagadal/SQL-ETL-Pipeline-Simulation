# Retail ETL Pipeline Project

This project uses SQL to simulate a real-world ETL (Extract, Transform, Load) pipeline. 
After being cleaned, transformed, and audited, raw e-commerce data will be loaded into production tables.